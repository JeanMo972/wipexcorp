{% extends 'base.html.twig' %}

{% block title %}Ajouter une adresse - AUTOBOOST{% endblock %}

{% block content %}
 <!-- Header-->
<img src="https://www.pierre-giraud.com/bootstrap-carrousel-slide-1.jpg" alt="" class="d-block w-100">     
<br>  
<br>  
<br>  
<h1>Gérer mes adresses</h1>
<a href="{{path('app_account_address_add')}}">Retour</a>
<hr>
{{form_start(form)}}
<div class="row">
  <div class="col-md-12">{{form_row(form.name)}}</div>
  <div class="col-md-6">{{form_row(form.firstname)}}</div>
  <div class="col-md-6">{{form_row(form.lastname)}}</div>
  <div class="col-md-6">{{form_row(form.compagny)}}</div>
  <div class="col-md-6">{{form_row(form.address)}}</div>
  <div class="col-md-6">{{form_row(form.postal)}}</div>
  <div class="col-md-6">{{form_row(form.city)}}</div>
  <div class="col-md-6">{{form_row(form.contry)}}</div>
  <div class="col-md-6">{{form_row(form.phone)}}</div>
</div>
{{form_end(form)}}   
{% endblock %}